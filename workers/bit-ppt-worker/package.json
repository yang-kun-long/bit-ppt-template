{
  "name": "bit-ppt-worker-spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "yaml": "^2.8.4"
  },
  "devDependencies": {
    "wrangler": "^4.51.0"
  }
}
